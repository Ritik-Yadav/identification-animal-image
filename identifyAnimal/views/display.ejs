<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Display</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    .result-container {
      text-align: center;
    }

    .prediction {
      font-size: 1.2em;
      margin-bottom: 10px;
      color: #007bff;
    }

    img {
      max-width: 100%;
      height: auto;
      margin-top: 20px;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body class="d-flex align-items-center justify-content-center">
  <div class="container text-center">
    <h1>Output</h1>
    <div class="result-container">
      <% if (predictions) { %>
        <div class="prediction">Predicted Animal: <%= predictions.className %></div>
        <div class="prediction">Probability: <%= (predictions.probability * 100).toFixed(2) %>%</div>
      <% } else { %>
        <div class="prediction">Unable to identify the animal.</div>
      <% } %>

    </div>
  </div>

</body>
</html>
